var examplearray = [2,-5,10,5,4,-10,6];
/
function process(input) {
	forEach (x) {
		forEach (y) {
			if x + y = 0
				newarray = push.(input[x],input[y])
			return newarray;
		}
	}
}
	

var results = process(examplearray);

console.log(results);
/
 function process(data)
 	